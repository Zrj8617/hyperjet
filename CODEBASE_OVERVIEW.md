# HyperUAV 代码情况总览（通俗版）

> 写给大二同学看的项目介绍。先讲"在干什么"，再讲"怎么组织的"，最后讲"做到哪了、还差什么"。

---

## 一、这个项目在干什么？

### 1.1 一句话总结

**让一群无人机（UAV）学会怎么飞、怎么分工，才能最快地帮地面用户（UE）算完任务。**

### 1.2 用生活场景打个比方

想象一个露天音乐节：

- **60 个观众（UE）** 在场地上走来走去，每个人时不时想用手机跑一个 AI 应用（比如实时美颜、语音翻译）。
- **5 架无人机（UAV）** 悬在空中，每架自带一个小服务器，能帮观众算这些任务。
- 观众的手机算力太弱，所以要把任务**"卸载"（offload）** 给无人机来算。
- 但无人机也不是万能的——它离你太远就收不到你的数据，它队列满了也接不了新活。

问题是：**无人机该往哪飞？每个任务该交给哪架无人机？**

这就是这个项目要解决的问题。

### 1.3 任务长什么样？—— DAG

观众提交的任务不是"一个活"，而是一串有先后依赖的子任务，叫 **DAG**（有向无环图）。

```
比如一个"语音翻译"任务 DAG：
  [录音采集] → [语音识别] → [机器翻译] → [结果返回]
                         ↘ [情感分析] ↗
```

- 必须先做完"录音采集"，才能开始"语音识别"；
- "机器翻译"要等"语音识别"做完；
- "情感分析"也要等"语音识别"做完；
- 最后"结果返回"要等"机器翻译"和"情感分析"都做完。

只有**所有子任务都算完，结果都送回观众手机**，这个 DAG 才算完成。

### 1.4 核心难点

1. **热点问题**：观众不是均匀分布的，某个区域人特别多、任务特别密集（"热点"），无人机要不要飞过去？
2. **依赖约束**：子任务之间有先后顺序，不能乱来。
3. **资源竞争**：多架无人机同时有空位，一个任务该给谁？多架无人机队列都快满了，怎么避免扎堆？
4. **移动决策**：无人机飞不飞、往哪飞，影响它能不能接到新任务。

### 1.5 项目的创新点——超图

普通图只能连两个点（"A 和 B 有关系"），但**超图的一条"超边"可以同时连好几个点**（"A、B、C 三者有关系"）。

这个项目用超图来表示任务之间**超出两两关系的复杂联系**，比如：

| 超边类型 | 通俗解释 | 例子 |
|---|---|---|
| DAG 依赖边 | 两个子任务有先后依赖 | "语音识别"必须在"录音采集"之后 |
| k-hop 邻域边 | 同一个 DAG 里隔了几步的"远房亲戚" | "录音采集"和"机器翻译"隔了两步，但属于同一个 DAG |
| 属性相似边 | 几个任务的"体型"差不多（输入大小、计算量等相似） | 三个计算量都很大的任务，可能需要相似的调度策略 |
| KaHyPar 分区边 | 用分区算法把任务分成几组，同组的应该分给同一架无人机 | 10 个任务分成 2 组，同组的尽量给同一架算，减少跨无人机传输 |

然后用**超图神经网络（HGNN）** 学出每个任务的"特征向量"（embedding），告诉后面的决策网络："这个任务是什么情况，它和谁关系近"。

### 1.6 训练方法——强化学习（MAPPO）

整个系统用**多智能体强化学习**来训练，具体用的是 **MAPPO**（Multi-Agent PPO）算法：

- **Movement Actor（移动决策网络）**：决定每架无人机往哪飞（悬停 / 左 / 右 / 上 / 下）。
- **Offloading Actor（卸载决策网络）**：决定每个就绪的任务交给哪架无人机。
- **Critic（评价网络）**：估计当前局面有多好，给两个 actor 提供学习信号。

训练过程就是反复跑仿真：无人机做决策 → 看结果好不好（奖励）→ 调整策略 → 再跑 → ……直到学会好策略。

奖励设计很简单：
- 完成一个 DAG → **加分**
- 任务花的时间长 → **扣分**
- 无人机飞来飞去耗电 → **扣分**
- 无人机算任务耗电 → **扣分**

---

## 二、代码怎么组织的？

### 2.1 目录结构（只看重要的）

```
HyperUAV/
│
├── config.py                  ← 所有参数都在这里（地图大小、无人机数量、奖励权重……）
│
├── environment/               ← 仿真世界：无人机、用户、任务、通信
│   ├── env.py                 ←   总环境，按时隙推进
│   ├── dag_tasks.py           ←   DAG 和子任务的定义、生命周期
│   ├── task_execution.py      ←   任务执行器（上传→排队→计算→回传）
│   ├── graph_builder.py       ←   超图构建（四类超边 + KaHyPar）
│   ├── assignment.py          ←   任务分配、合法性检查、临时预约
│   ├── uavs.py                ←   无人机类
│   ├── user_equipments.py     ←   用户设备类（移动模型）
│   ├── comm_model.py          ←   通信模型（距离→速率→时间）
│   └── metrics.py             ←   指标统计（完成率、延迟、能耗……）
│
├── marl_models/               ← AI 大脑：神经网络和训练逻辑
│   ├── hgnn/                  ←   超图神经网络
│   │   └── clean_incidence.py ←     核心：关联矩阵 + HGNN 消息传递
│   └── mappo/                 ←   MAPPO 相关
│       ├── clean_movement_actor.py      ← 移动决策网络
│       ├── clean_offloading_actor.py    ← 卸载决策网络
│       ├── clean_ppo.py                 ← PPO 训练逻辑、Critic
│       ├── clean_slot_orchestrator.py   ← 把一个时隙的流程串起来
│       └── clean_trainer.py             ← 训练循环、保存模型、记录日志
│
├── scripts/                   ← 运行脚本
│   ├── train_clean_mainline.py ←  正式训练入口（用这个！）
│   ├── eval_clean_mainline.py  ←  正式评估入口
│   ├── smoke_clean_*.py        ←  各种小测试（60+ 个，检查每个模块能不能跑通）
│   └── ...
│
├── docs/                      ← 设计文档（最重要的参考）
│   ├── hyperuav_clean_mainline_design.md  ← 权威设计文档
│   └── superpowers/            ←   实现计划、实验报告
│
├── analysis/                  ← 离线分析脚本
├── plotting/                  ← 画图脚本
├── utils/                     ← 日志、绘图工具
└── third_party/kahypar/       ← KaHyPar 分区器（第三方程序）
```

### 2.2 一个"时隙"（slot）发生了什么？

每 5 秒是一个时隙，以下是每个时隙的流程，用大白话讲：

```
┌──────────────────────────────────────────────────┐
│  1. 准备阶段                                      │
│     观众走一步 → 有人提交新任务 → 确认哪些任务可以做  │
│     → 给当前任务建超图 → HGNN 算出每个任务的特征      │
├──────────────────────────────────────────────────┤
│  2. 移动阶段                                      │
│     每架无人机决定：悬停？往左？往右？往前？往后？      │
│     （飞完后的位置就是本时隙的"服务位置"）             │
├──────────────────────────────────────────────────┤
│  3. 卸载阶段                                      │
│     对每个"可以做"的任务，依次选一架无人机来执行        │
│     （选的时候会考虑：离得够不够近？队列有没有满？）     │
│     注意：只是"预约"，还没真正开始算                   │
├──────────────────────────────────────────────────┤
│  4. 执行与结算阶段                                  │
│     把所有预约的任务正式提交给执行器                    │
│     → 上传数据 → 排队 → 计算 → 结果送回观众           │
│     → 算奖励 → 记指标 → 准备下一个时隙                │
└──────────────────────────────────────────────────┘
```

### 2.3 任务的一生（生命周期）

```
等待依赖 ──→ 就绪未分配 ──→ 服务中 ──→ 回传中 ──→ 已完成
               │                │
         （还没轮到你）    （正在上传/排队/计算）
```

- **等待依赖**：前面的子任务还没做完，你不能开始。
- **就绪未分配**：前置任务都做完了，等无人机来接你。
- **服务中**：已经交给某架无人机了，正在上传数据 → 排队 → 计算。
- **回传中**：只有"出口任务"（sink）会进入这个状态——算完了但结果还没送回观众手机。
- **已完成**：搞定了！（非出口任务：算完就行；出口任务：结果送回手机才行）

---

## 三、现在做到哪了？

### 3.1 已经完成的事

- ✅ 仿真环境搭好了（无人机、用户、任务生成、通信、执行、指标统计）。
- ✅ 超图构建搭好了（四类超边、KaHyPar 分区、缓存机制）。
- ✅ 超图神经网络（HGNN）搭好了（支持 4 种变体）。
- ✅ 移动决策网络和卸载决策网络搭好了。
- ✅ PPO 训练框架搭好了（critic、GAE、loss 计算）。
- ✅ 各种小测试（smoke test）写了 60+ 个，能单独检查每个模块。
- ✅ 多环境并行采样搞好了（能在服务器上同时跑多个环境加速训练）。
- ✅ 服务器验证流程文档写好了（三道门：torch 测试 → 最小训练测试 → 短训练检查）。

### 3.2 还没做的事 / 还在解决的问题

#### 环境相关

1. **你电脑上可能跑不了训练。** 训练需要 PyTorch + GPU，本地如果没有装好 torch，只能跑不需要神经网络的测试。正式训练要去服务器上跑。

2. **正式训练前必须过三道门。** 不能直接开跑大实验，必须先通过三个小测试（就像打游戏先过新手教程），全部通过才算环境没问题。

#### 算法相关

3. **一个时隙里多个卸载决策的"功劳"算不清楚。** 现在的做法是：一个时隙里做了好几个卸载决策，但算奖励时把它们当成"一锅粥"一起评，分不清哪个决策好、哪个决策差。目前正在开发一种新方法（Decision-level PPO），让每个决策都有自己的"功劳评分"。

4. **无人机可能偷懒只会悬停。** 训练初期，无人机可能发现"不动就不犯错"，于是全部选择悬停不动。这是训练时需要观察和防范的问题。

5. **"任务能不能分给这架无人机"的判断必须处处一致。** 代码里有三个地方做这个判断（候选过滤、临时预约、执行器提交），如果规则不一致就会出现"以为可以分配但执行器拒了"的 bug。这种情况不应该通过惩罚让 AI 自己学，而是要把规则写对。

6. **奖励的"尺子"还需要校准。** 奖励里用到的参考值（多长时间算"长"、多少能耗算"多"）需要在正式训练前用随机策略跑一次来确定，训练过程中不能随便改。

7. **并行采样用几个环境好？** 测过 1/2/4/8 个并行环境，8 最快但每次更新的数据量更大、更新次数更少，还不确定对学习效果的影响。

#### 实验相关

8. **对照组实验还没做。** 论文需要对比"用超图 vs 不用超图"、"用普通图 vs 用超图"等消融实验，目前都没实现，只跑主方法。

9. **正式评估流程还没跑过。** 正式测试需要先让任务正常产生一段时间（arrival phase），然后停止新任务、等所有剩余任务做完（drain phase），这样统计才更准确。

10. **一些实验性功能默认关闭。** 比如反事实批评器（counterfactual critic）、滞后残差 Q（lagged residual-Q）等，都是"先写好代码但不开"，不影响当前主方法。

#### 代码清理

11. **代码里有不少"旧版本"遗留。** 早期写了很多后来不用了的算法（MADDPG、MATD3、MASAC 等），还留着但**不能用于当前主方法**。新手容易误用。

12. **配置文件里有废弃参数。** `config.py` 里有些参数名标注了"Deprecated"（已废弃），改它们不会影响当前系统，但容易改错地方。当前系统只认以 `ENABLE_` 开头的四个超边开关。

---

## 四、怎么跑起来？

### 最简单的验证（不需要 GPU）

```bash
# 检查环境基本逻辑（不需要 PyTorch）
python scripts/smoke_clean_env.py
python scripts/smoke_clean_execution.py
python scripts/smoke_clean_graph.py
```

### 需要 GPU 的完整验证（去服务器上跑）

```bash
# 第一步：检查 PyTorch 和模型能不能跑通
python scripts/smoke_clean_server_torch.py

# 第二步：跑 3 个 episode 的最小训练
python scripts/train_clean_mainline.py --smoke --episodes 3 --max-steps-per-episode 20 --rollout-horizon 5 --run-name smoke

# 第三步：跑 100 个 episode 的短训练检查
python scripts/run_clean_sanity.py --episodes 100 --max-steps-per-episode 200 --rollout-horizon 128 --seed 0 --run-name sanity_seed0
```

**三步都通过后**，才能跑正式的大规模训练。

### 正式训练示例

```bash
python scripts/train_clean_mainline.py --episodes 500 --max-steps-per-episode 500 --rollout-horizon 128 --seed 0 --run-name hypergraph_seed0 --output-dir logs/clean_mainline
```

---

## 五、关键文件速查

| 你想了解…… | 去看这个文件 |
|---|---|
| 权威算法设计（论文思路） | `docs/hyperuav_clean_mainline_design.md` |
| 服务器怎么跑实验 | `docs/hyperuav_clean_server_runbook.md` |
| 完整实验启动步骤 | `docs/hyperuav_clean_experiment_launch.md` |
| 所有参数在哪里改 | `config.py` |
| 仿真环境怎么运转 | `environment/env.py` |
| 任务和 DAG 怎么定义 | `environment/dag_tasks.py` |
| 任务怎么执行 | `environment/task_execution.py` |
| 超图怎么构建 | `environment/graph_builder.py` |
| 超图神经网络 | `marl_models/hgnn/clean_incidence.py` |
| 无人机移动决策 | `marl_models/mappo/clean_movement_actor.py` |
| 任务卸载决策 | `marl_models/mappo/clean_offloading_actor.py` |
| PPO 训练和 Critic | `marl_models/mappo/clean_ppo.py` |
| 训练入口 | `scripts/train_clean_mainline.py` |

---

## 六、名词解释

| 缩写/术语 | 大白话解释 |
|---|---|
| UAV | 无人机，天上飞的小服务器 |
| UE | 用户设备，就是地上的手机 |
| DAG | 有向无环图，描述任务之间的先后依赖关系 |
| Offloading | 卸载，把手机上的任务交给无人机算 |
| Slot / 时隙 | 5 秒一个的时间片，仿真按此时钟走 |
| Hotspot / 热点 | 用户扎堆的区域，任务特别多 |
| Hypergraph / 超图 | 比普通图更灵活的图，一条边可以连多个点 |
| HGNN | 超图神经网络，用超图结构来学任务特征 |
| MAPPO | 多智能体近端策略优化，训练多个 AI 协作的算法 |
| Actor | 做决策的网络（选动作） |
| Critic | 评估局面好坏的网络（打分） |
| PPO | 近端策略优化，一种稳定更新策略的方法 |
| KaHyPar | 一个分区算法，把任务分成几组 |
| Smoke test | 冒烟测试，最小化的"能不能跑通"检查 |
| GAE | 广义优势估计，算"这个动作比平均好多少"的方法 |
| Embedding | 特征向量，把任务变成一串数字让网络理解 |
| Incidence matrix | 关联矩阵，记录"哪个点属于哪条超边"的表格 |
